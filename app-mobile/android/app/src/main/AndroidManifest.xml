<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Requise pour tous les appels réseau de l'app (API_BASE_URL, #18) : présente
         par défaut dans les manifestes debug/profile (scaffold Flutter, hot reload),
         mais absente ici jusqu'à présent : un flutter build apk en mode release
         n'hérite pas de ces manifestes et ne pouvait donc joindre aucun serveur,
         quelle que soit l'URL configurée. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Imprimante ticket Bluetooth (#160, flutter_thermal_printer). Deux jeux de
         permissions, comme recommandé par Android pour couvrir toutes les versions
         du parc de bornes :
           - avant Android 12 (API 30 et moins) : BLUETOOTH/BLUETOOTH_ADMIN classiques,
             plus ACCESS_FINE_LOCATION (la recherche BLE y était historiquement liée à
             la localisation) ;
           - Android 12 et plus (API 31+) : BLUETOOTH_SCAN/BLUETOOTH_CONNECT, permissions
             dédiées qui remplacent les précédentes. usesPermissionFlags="neverForLocation"
             dispense de la permission de localisation runtime, la recherche ne servant
             qu'à trouver l'imprimante (aucune donnée de position dérivée du scan). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <application
        android:label="coiflink_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
