# Adapters UI (app-mobile)

Adapter **entrant** (driving) : widgets et écrans Flutter (présentation), point
d'entrée du framework. `app.dart` expose `CoifLinkApp`. Les écrans appellent les
cas d'usage de `application/` ; aucune règle métier ici (ADR-0008).
