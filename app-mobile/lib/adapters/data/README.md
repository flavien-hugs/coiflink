# Adapters data (app-mobile)

Adapters **sortants** (driven) : clients de l'API backend FastAPI (ADR-0003),
stockage local, etc. Implémentent les ports déclarés dans `application/`.
Vide au démarrage (#2).
