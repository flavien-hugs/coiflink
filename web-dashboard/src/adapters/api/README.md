# Adapters API (web-dashboard)

Adapters **sortants** (driven) : clients HTTP vers l'API backend FastAPI
(ADR-0003) et autres services externes. Implémentent les ports déclarés dans
`application/`. Vide au démarrage (#2).
