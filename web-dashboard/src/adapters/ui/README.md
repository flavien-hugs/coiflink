# Adapters UI (web-dashboard)

Composants React (présentation) consommés par le routage **`app/`** de Next.js,
qui joue le rôle d'adapter entrant du framework. Les composants appellent les
cas d'usage de `application/` ; ils ne contiennent pas de règle métier (ADR-0008).
